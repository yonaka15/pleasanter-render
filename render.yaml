databases:
  - name: pleasanter_db
    plan: basic-256mb
    region: singapore
    databaseName: pleasanter
    user: pleasanter

services:
  - type: web
    name: pleasanter
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: basic-256mb
    region: singapore
